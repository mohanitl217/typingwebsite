// Root entry point for Node hosting (Hostinger, etc.).
// Hostinger's "Entry file" is set to server.js, so this file boots the API
// + serves the built React app from /dist.
import './server/index.js'
